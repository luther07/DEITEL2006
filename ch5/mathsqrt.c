/* stdfun.c
	Testing sqrt function which returns doubles. */
#include <stdio.h>
#include <math.h>

/* function main begins program execution */
int main( void )
{
   printf( "%.1f\n", sqrt( 900.0 ) );
   return 0; /* indicates that the program ended successfully */

}  /* end function main */
